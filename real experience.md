# 反思驱动的经验复用：为大型语言模型构建可寻址的自进化认知架构

## 摘要

大型语言模型已经具备较强的推理能力，但通常不会像人类一样主动联想到“这和我以前处理过的问题相似”，也不会自然地复用过去案例中可迁移的部分。本文研究一种经验复用训练范式：事实和历史案例可以被索引保存，而“何时联想、调用什么、复用哪些部分、如何修改并验证”则被训练成模型自身的能力。

本文不把经验等同于一段固定的总结文本，也不再将低困惑度、输出速度或单条轨迹长度直接视为经验价值。研究重点是构建一个经验调用专家模型，使其能够在面对新问题时主动联想相关历史案例，复用其中适用部分，并针对当前问题的差异进行修改。随后利用该专家生成的数据，将这种能力蒸馏到目标模型中。

整体框架包含：
1. **索引记忆**：保存事实、历史题目或项目案例，并保留可追踪的来源标识；
2. **经验调用专家**：训练模型主动判断是否需要调用记忆、选择相关案例、局部复用、识别差异并验证结果；
3. **专家数据蒸馏**：由专家生成经验联想与复用轨迹，再将该行为蒸馏到目标模型；
4. **内部化调用能力**：部署时不要求用户显式提供经验提示，模型应能够自主触发记忆访问机制。

本文的核心目标不是让模型保存一套经验总结，而是让模型形成类似人类的“过去做过类似事情—想起相关案例—复用有效部分—修改不同部分—检查结果”的能力。

**关键词**：大型语言模型；经验复用；索引记忆；关联召回；强化学习；DeepSeek-R1；Engram；知识蒸馏

---

## 1. 引言

当前，基于可验证奖励的强化学习（RLVR）已成为提升大模型推理能力的重要范式。模型能够在单次任务中进行反思、验证和回溯，但一次推理结束后，相关经历通常不会以可寻址的形式被保留。模型下一次遇到相似问题时，往往仍然需要从零开始搜索。

人类解决问题时并不总是先总结一条抽象原则。很多时候，人类只是想起“以前做过一个类似项目”或“这道题和之前那道题很像”，然后复用原方案中适用的部分，再处理当前问题的差异。这种行为同时包含两种能力：

- **案例记忆**：知道过去发生过什么，并能够追溯其来源；
- **经验联想**：在新问题出现时，主动判断是否值得回忆，并调用相关案例。

本文将两者分开处理：事实和历史案例可以进入索引记忆；经验本身不被定义为一个必须显式存储的总结对象，而被视为模型对记忆进行联想、调用、修改和验证的能力。

DeepSeek-R1 表明，强化学习能够使模型涌现反思、验证和策略切换行为。Engram 等条件记忆架构则提示我们，可以将高容量事实存储与 Transformer 的计算过程分离。本文尝试将这两条路线结合起来：使用可寻址记忆保存历史事实和案例，使用训练使模型学会主动调用记忆，而不是每次都依赖外部提示词。

---

## 2. 研究定位与相关工作

### 2.1 推理强化与专家蒸馏

DeepSeek-R1 及其蒸馏路线说明，先训练一个具有稳定推理行为的专家，再使用专家生成的高质量轨迹训练目标模型，是一种可行的能力迁移方式。本文沿用这一基本思路，但专家的目标从单纯提高解题正确率，扩展为学习主动的经验联想与局部复用。

### 2.2 索引记忆与条件访问

Memorizing Transformers、RETRO 等工作展示了通过近邻或外部索引访问历史信息的方法。MemoryLLM、Titans 等工作进一步探索了模型的动态记忆和测试时记忆。DeepSeek Engram 则提供了“高容量记忆通过条件查表参与计算”的架构启发。

本文与普通 RAG 的区别在于：研究重点不只是把检索结果放进上下文，而是训练模型形成以下内部策略：

```text
当前问题 → 判断是否需要回忆 → 触发索引访问 → 复用相关案例 → 修改差异 → 验证结果
```

索引返回的事实或案例可以作为计算条件，但“何时调用、调用什么、如何复用”属于模型要学习的能力。

### 2.3 本文的研究问题

本文主要研究三个问题：

1. 模型能否在不对所有问题都调用记忆的情况下，主动识别需要回忆的任务；
2. 模型能否从历史案例中复用局部方案，而不是机械复制完整答案；
3. 通过专家生成数据蒸馏后，目标模型能否在没有显式经验提示时保留这种行为。

---

## 3. 方法论

本文采用“索引记忆—经验调用专家—目标模型蒸馏”的三部分框架。

### 3.1 索引记忆：保存事实与历史案例

记忆模块保存的是可追踪的事实或案例，而不是不可解释的经验分数。每条记录至少包含：

```json
{
  "memory_id": "case_017",
  "source_problem_id": "gsm8k_0381",
  "content": "历史题目、项目方案或事实内容",
  "index_key": "可用于条件寻址的表示",
  "metadata": {
    "domain": "algebra",
    "created_at": "...",
    "source": "..."
  }
}
```

`source_problem_id` 用于保留来源链路。训练完成后，即使案例的作用被部分写入模型参数，仍可通过训练记录知道其来源。

索引访问可以采用 Engram 类的条件查表、稀疏路由或其他可寻址实现。第一版实验不要求直接修改底层 CUDA 算子，可以先使用可替换的索引接口验证训练逻辑。

### 3.2 经验调用专家

专家模型需要学习的不是“背诵经验总结”，而是以下行为：

1. 先尝试直接处理简单问题；
2. 判断当前问题是否与历史案例存在足够相似性；
3. 只有在需要时触发记忆访问；
4. 找到可复用的局部结构或方案；
5. 识别当前问题与历史案例的不同；
6. 修改旧方案并进行结果验证。

其抽象流程为：

```text
当前问题
  ↓
快速直接推理
  ↓
是否需要回忆？
  ├─ 否：直接回答
  └─ 是：访问索引记忆
          ↓
      复用相关部分
          ↓
      修改差异并验证
```

### 3.3 训练数据形式

训练样本可以来自历史案例与新问题的配对，不要求先生成抽象经验总结。一个基本样本包含：

```json
{
  "past_case": "过去解决过的题目或项目",
  "new_problem": "当前的新问题",
  "retrieval_decision": "调用 / 不调用",
  "reusable_part": "可以复用的部分",
  "difference": "当前问题与旧案例的差异",
  "adapted_solution": "修改后的解法",
  "verification": "对结果的检查"
}
```

必须同时包含三类样本：

- 相似且应该调用记忆的样本；
- 简单或无关、应该直接处理的样本；
- 表面相似但关键约束不同、必须修改旧方案的样本。

### 3.4 强化学习与专家蒸馏

强化学习用于强化以下行为结果：

- 正确判断是否需要记忆；
- 调用相关案例而不是无关案例；
- 复用后能够正确适配当前问题；
- 记忆调用不会导致简单问题过度推理；
- 最终答案通过外部验证。

第一版不把“加速度奖励”作为核心目标。Token 数、PPL 或调用次数可以作为辅助诊断指标，但不能单独代表经验价值。

训练流程为：

```text
基础模型
  ↓
带索引记忆接口的经验调用专家训练
  ↓
专家在新问题上生成联想、复用与验证轨迹
  ↓
数学/任务正确性过滤
  ↓
蒸馏到目标模型
  ↓
测试无显式经验提示时的自主调用行为
```

### 3.5 来源追踪与内部化

经验内容在运行时可以通过内部索引访问，但来源信息在训练阶段必须保留：

```text
memory_id ← source_problem_id ← expert trajectory ← distillation sample
```

这样可以同时满足：

- 运行时经验不必作为用户 prompt 输入；
- 实验者仍能知道某种行为来自哪些历史案例。

需要注意，普通参数训练会混合多个来源，训练完成后不能保证从模型权重唯一反推出某一道题。因此来源追踪必须依赖训练日志、索引记录和蒸馏数据，而不能只依赖最终权重分析。

---

## 4. 实验设置

### 4.1 模型与数据

- **基础模型**：选择具备数学或代码推理能力的开源模型；
- **历史案例库**：由已验证的问题、项目方案或事实组成，并保留唯一来源 ID；
- **新问题集**：与历史案例分离，包含简单问题、结构相似问题、表面相似但约束不同的问题和无关问题；
- **专家模型**：接收可寻址记忆接口，通过监督微调和必要的 GRPO 微训练学习调用行为；
- **目标模型**：使用专家筛选出的轨迹进行蒸馏，并在无外部经验提示下测试。

第一阶段可以继续使用 GSM8K、MATH 或代码任务进行基础能力验证，但不应把边界题数量或 500 题视为理论必要条件。

### 4.2 最小实验对照

为了保持实验简洁，第一版只需要比较：

1. 无记忆，直接回答；
2. 外部显式提供案例；
3. 模型通过索引接口自主访问案例；
4. 蒸馏后的模型在无显式案例提示下回答。

重点观察模型是否：

- 对简单题不过度调用记忆；
- 对相似新题能够调用相关案例；
- 能够修改旧案例中的不适用部分；
- 在无显式提示时保留类似行为。

### 4.3 评估指标

核心指标为行为正确性和调用选择，而不是单独的 PPL 或 Token 数：

- 最终任务正确率；
- 记忆调用准确率：该调用时调用、不该调用时不调用；
- 相关案例命中率；
- 复用后适配正确率；
- 表面相似但约束不同任务上的错误复用率；
- 无显式经验提示时的行为保留率；
- 历史案例来源可追踪率。

PPL、生成长度、调用延迟和记忆命中频率作为辅助指标，用于分析效率和失败原因。

---

## 5. 预期成果与讨论

### 5.1 预期成果

1. 构建一个可寻址的事实与历史案例记忆接口；
2. 训练一个能够主动判断是否调用记忆的经验调用专家；
3. 生成包含“联想—局部复用—差异修改—验证”的专家轨迹；
4. 将该行为蒸馏到目标模型，并测试其在无显式经验提示下的保留情况；
5. 建立从模型行为到历史案例来源的训练 lineage。

### 5.2 研究边界

本文暂不声称：

- 低 PPL 必然代表更好的经验；
- 模型权重中保存了可唯一定位的某条经验；
- 检索到案例就等于模型真正复用了案例；
- 单次准确率提升就能证明产生了举一反三能力。

本文更谨慎的研究主张是：

> 通过索引记忆与专家蒸馏，能否使模型学会在需要时主动联想历史案例，并将其中适用部分迁移到新的问题中。

### 5.3 局限性

- Engram 类架构与动态经验记忆并不完全等价，需要通过实验确定适合的接口；
- “主动调用”可能只是表面行为，必须结合调用日志和反事实条件分析；
- 记忆内容与模型参数之间的职责边界需要进一步明确；
- 经验来源可以被记录，但参数内化后通常无法实现严格的一对一溯源；
- 训练数据必须避免把完整答案复制误认为经验复用能力。

---

## 6. 结论

本文将研究主线从“通过加速度奖励推动经验固化”调整为“通过索引记忆承载事实与案例，通过训练内化主动联想和经验复用能力”。

最终架构为：

```text
事实 / 历史案例 → 可寻址索引记忆
调用时机 / 关联判断 / 局部复用 / 差异修改 → 模型能力
专家行为 → 目标模型蒸馏
来源信息 → 训练 lineage 持久化
```

这种拆分既保留了案例的可追踪性，也避免把经验简单地退化为 prompt 中的一段总结文本。加速度奖励、PPL 和 Token 长度可以作为辅助实验变量，但不再作为研究核心。真正需要验证的是：模型能否像人类一样，在简单问题上直接作答，在复杂或相似问题上主动想起过去的案例，并在复用的基础上解决新的问题。

---

## 参考文献

1. **DeepSeek-AI**. *DeepSeek-R1: Incentivizing Reasoning Capability in Large Language Models via Reinforcement Learning*.
2. **DeepSeek-AI**. *DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models*.
3. **Borgeaud et al.** *Improving language models by retrieving from trillions of tokens* (RETRO).
4. **Wu et al.** *Memorizing Transformers*.
5. **Wang et al.** *MEMORYLLM: Towards Self-Updatable Large Language Models*.
6. **Behrouz et al.** *Titans: Learning to Memorize at Test Time*.
7. **DeepSeek-AI**. *Conditional Memory via Scalable Lookup: A New Axis of Sparsity for Large Language Models* (Engram).
8. **Shinn et al.** *Reflexion: Language Agents with Verbal Reinforcement Learning*.
